<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App theme: fully transparent so Compose controls all drawing.
         The radar canvas uses a dark charcoal background (#0B0C10) defined in MayaraTheme.kt -->
    <!-- Use Theme.Material.NoActionBar (API 21+) as parent; fullscreen handled via item -->
    <style name="Theme.Mayara" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
